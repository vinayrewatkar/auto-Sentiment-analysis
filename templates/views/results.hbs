<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <!-- Add this code to your results page (e.g., results.hbs) -->
<h2>Emotion Sentiment Analysis Results</h2>

<!-- Display pie chart -->
<img src="{{ images.pieChart }}" alt="Pie Chart">

<!-- Display bar graph -->
<img src="{{ images.barGraph }}" alt="Bar Graph">

<!-- Display points graph -->
<img src="{{ images.pointsGraph }}" alt="Points Graph">

<!-- Display the script output if needed -->
<pre>{{ output }}</pre>

</body>
</html>